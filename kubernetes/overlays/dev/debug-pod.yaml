apiVersion: v1
kind: Pod
metadata:
  name: debug-tools
  labels:
    app: debug
spec:
  containers:
  - name: debug
    image: curlimages/curl
    command: ["/bin/sleep", "3650d"] # Sover bare, så den holder sig i live